---
# Install InfluxDB on Arch Linux
- name: Install InfluxDB from community repo
  pacman:
    name: influxdb
    state: present
    update_cache: yes

- name: Ensure InfluxDB is started and enabled
  service:
    name: influxdb
    state: started
    enabled: yes
